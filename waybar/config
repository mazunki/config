[{
	"layer": "bottom",
	"position": "top",
	"height": 50,
	"spacing": 4,
	"margin-top": 2,
	"margin-bottom": 2,
	"output": ["HDMI-A-1", "HDMI-A-2"],

	"modules-center": ["custom/windowinfo"],
	"modules-left": ["cpu", "memory", "network#eth", "network#wlp", "temperature"],

	"custom/windowinfo": {
		//"exec": "~/.local/bin/wl-info-current",
		"interval": 1
	},
	"cpu": {
		"format": "{usage}% ",
		"tooltip": false,
		"interval": 1
	},
	"memory": {
		"format": "\t{used:3.2f} GiB",
		"interval": 5
	},
	"network#eth": {
		"interface": "enp*",
		"format-ethernet": "{ipaddr}  ",
		"tooltip-format": "{ifname} via {gwaddr}",
		"format-linked": "{ifname} (No IP)",
		"format-disconnected": "Disconnected ⚠",
		"format-alt": "{ifname}: {ipaddr}/{cidr}",
	},
	"network#wlp": {
		"interface": "wlp*",
		"format-wifi": "{essid} ({signalStrength}%) ",
		"tooltip-format": "{ifname} via {gwaddr} ",
		"format-linked": "{ifname} (No IP)",
		"format-disconnected": "Disconnected ⚠",
		"format-alt": "{ifname}: {ipaddr}/{cidr}",
	},
	"temperature": {
		// "thermal-zone": 2,
		// "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
		"critical-threshold": 80,
		// "format-critical": "{temperatureC}°C {icon}",
		"format": "{temperatureC}°C {icon}",
		"format-icons": [""]
	}
},
{
	"layer": "top",
	"position": "bottom",
	"height": 50,
	"spacing": 4,
	"margin-top": 5,
	"margin-bottom": 10,

	"modules-left": ["custom/launcher", "custom/terminal", "sway/workspaces", "custom/spawnworkspace"],
	//"modules-center": ["custom/media"],
	"modules-right": ["custom/updater", "pulseaudio", "sway/language", "tray", "clock", "custom/power"],

	// "sway/workspaces": {
		// "disable-scroll": false,
		// "all-outputs": false,
		// "format": "({value}): {icon}",
		// "persistent_workspaces": {
		//}
	// },
	"custom/spawnworkspace": {
		"format": "+",
		"on-click": "swaymsg -t get_workspaces -r | jq '[.[].num] | max + 1' | xargs swaymsg workspace number"
	},
	"custom/terminal": {
		"format": "T",
		"on-click": "foot",
	},
	"sway/mode": {
		"format": "<span style=\"italic\">{}</span>"
	},
	"idle_inhibitor": {
		"format": "{icon}",
		"format-icons": {
			"activated": "",
			"deactivated": ""
		}
	},
	"tray": {
		"icon-size": 30,
		"spacing": 10
	},
	"clock": {
		"tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
		"format": "{:%F %T}",
		"format-alt": "{:%F %T}",
		"interval": 1
	},
	"backlight": {
		// "device": "acpi_video1",
		"format": "{percent}% {icon}",
		"format-icons": ["", ""]
	},
	"pulseaudio": {
		// "scroll-step": 1, // %, can be a float
		"format": "{desc} {volume}% {icon}",
		"format-bluetooth": "{volume}% {icon}",
		"format-bluetooth-muted": "{icon} {format_source}",
		"format-muted": "{format_source}",
		"format-source": "",
		"format-source-muted": "",
		"format-icons": {
			"headphone": "",
			"hands-free": "",
			"headset": "",
			"phone": "",
			"portable": "",
			"car": "",
			"default": ["", "", ""]
		},
		"on-click": "pavucontrol",
		"on-click-right": "/usr/bin/helvum"
	},
	"custom/media": {
		"format": "{icon} {}",
		"return-type": "json",
		"max-length": 25,
		"format-icons": {
			"spotify": " ",
			"default": " "
		},
		"escape": true,
		"exec": "$HOME/.config/system_scripts/mediaplayer.py 2> /dev/null",
		"on-click": "playerctl play-pause"
	},
	"custom/launcher":{
		"format": "🐧",
		"on-click": "wofi --gtk-dark --dmenu --alow-images --show drun",
		"on-click-right": "killall wofi"
	},
	"custom/power":{
		"format": "⚡",
		"on-click": "",
		"on-click-right": ""
	},
	"custom/updater":{
		"format": "  {} Updates",
		"exec": "emerge --sync",
		"exec-if": "",
		"interval": 3600,
		"on-click": ""
	}
}]
