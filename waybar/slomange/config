[{
    "layer": "top",
    "position": "bottom",
    "modules-left": ["sway/workspaces", "sway/mode", "tray"],
    "modules-right": ["battery", "backlight", "pulseaudio", "network#eth", "network#wifi", "clock"],
    "clock": {
    	"interval": 1,
		"format": "{:%c}",
    },
    "battery": {
	"format": "🔋{capacity}%",
    },
    "backlight": {
	"format": "☀ {percent}%",
    },
    "pulseaudio": {
	"format": "🔊 {volume}%",
	"on-click": "pavucontrol",
    },
    "network#eth": {
    	"interface": "enp*",
        "format-ethernet": "{ipaddr} / {cidr} via {gwaddr} ",
        "format-disconnected": "—",
        "tooltip-format": "{ifname} via {gwaddr}",
        "max-length": 50,
	"on-click": "alacritty --hold -e ip -c addr show ${ifname}",
    },
    "network#wifi": {
    	"interface": "wlp*",
        "format-wifi": "{ipaddr}/{cidr} {essid} ({signalStrength}%) 📶",
        "format": "— 📶",
        "tooltip-format": "{ifname} via {gwaddr}",
	"on-click": "alacritty --hold -e ip -c addr show ${ifname}",
        "max-length": 50,
    },
    "tray": {
    	"icon-size": 30,
	"spacing": 10
    }
}]
